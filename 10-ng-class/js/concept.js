/*
	Let's pretend that we are using jquery library to manipulate class styles.
*/
var element = $('.some-element');

if(condition) {
	element.addClass('a');
} else if(anotherCondition) {
	element.addClass('b');	
}

/*
	In this module will show you how to do the same thing in a 
	simplified way with angularJS
*/